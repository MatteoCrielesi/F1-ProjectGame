// mp_sync.dart
import 'mp_lobby.dart';

/// Applica lo stato ricevuto (client) ai modelli di gioco
void applyStateToLocal(MpPlayer p, /* ad esempio tuo modello visivo */) {
  // ad esempio: posizionare la macchina del giocatore p in coordinate (p.x, p.y)
}
